1. قراءة الرقم من المستخدم:
   - استخدم دالة `input()` لطلب إدخال الرقم من المستخدم.
   - حول الإدخال إلى عدد صحيح باستخدام `int()`.

2. التحقق من كون الرقم زوجيًا أو فرديًا:
   - استخدم عامل الباقي `%` لمعرفة إذا كان الرقم يقبل القسمة على 2 بدون باقي.
   - إذا كان الباقي 0، فالرقم زوجي؛ وإلا، فهو فردي.

3. طباعة النتيجة:
   - استخدم جملة شرطية `if-else` لطباعة "Number is even" إذا كان الرقم زوجيًا، أو "Number is odd" إذا كان فرديًا.

الكود النهائي يبدو كالتالي:

```python
# قراءة الرقم من المستخدم
number = int(input("أدخل رقمًا: "))

# التحقق من كون الرقم زوجيًا أو فرديًا
if number % 2 == 0:
    print("Number is even")
else:
    print("Number is odd")
```
